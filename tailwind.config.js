module.exports = {
    purge: ['./dist/**/*.html', './dist/**/*.js'],
    darkMode: 'class', // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
